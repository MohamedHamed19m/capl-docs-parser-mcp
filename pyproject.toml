[project]
name = "capl-docs-parser-mcp"
version = "0.1.0"
description = "A local MCP server for Gemini that parses Vector CAPL markdown documentation."

dependencies = [
    "fastmcp",
    "scikit-learn",
    "numpy"
]